import Login from "./_components/Login"

export const metadata = {
    title: "Cafe App | Login",
    description: "Login to your account",
}

export default function loginPage() {
    return <Login />
}